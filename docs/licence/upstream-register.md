# Upstream register

No upstream source has been imported into Promaly yet.

When source is imported, add its upstream URL, commit SHA, licence, file path, changes and test coverage here before merging.

## Behavioural references (not imports)

`docs/upstream-notes/interaction-spec.md` is a clean-room specification written
from studying mature issue trackers (Huly, Linear) as reference for functional
interaction patterns. It reproduces no source, markup, CSS values, or copy; every
token value and component decision in it is Promaly's own. It is not an import
and creates no licence obligation.
